# FasterHQ Homebrew Tap

```
brew install fasterhq/tap/faster
```

Hosts the `faster` CLI (chat-first terminal client for a Faster box). Signed
Developer ID binaries are attached to this repo's releases; source is private.
